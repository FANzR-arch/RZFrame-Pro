职责：前端渲染层总目录，统管所有不在主进程内运行的业务代码。
地位：Renderer Process Root

成员清单：
- `core/`: 核心业务逻辑，负责状态管理、绘图引擎和导出行为
- `ui/`: 界面交互管理，负责 DOM 事件绑定、组件渲染和拖拽管理
- `config/`: 静态常量与硬代码配置表
- `locales/`: 多语言字典映射表
- `styles/`: 全局 CSS 及混入变量
- `utils/`: 无副作用的纯函数工具箱
